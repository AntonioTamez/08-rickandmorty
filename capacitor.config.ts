import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.rickandmorty',
  appName: 'rickandmorty',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
