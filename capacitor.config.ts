import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.antoniotamez.rickandmorty',
  appName: 'rickandmorty',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
